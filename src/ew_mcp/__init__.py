"""MCP server for the Education-to-Workforce Framework data."""

# Advertised as serverInfo.version on every connection, so a deployed instance
# can be identified from the client side. Kept in step with pyproject.toml.
__version__ = "0.1.0"
